
<div class="available-languages">
  <ul>
    <li class="translation">
      <% if params[:locale] == 'en' %>
        <span>English</span>
      <% else %>
        <%= link_to 'English', url_for(locale: 'en'), lang: 'en', hreflang: 'en' %>
      <% end %>
    </li>
    <li class="translation last">
      <% if params[:locale] == 'cy' %>
        <span>Deutsch</span>
      <% else %>
        <%= link_to 'Deutsch', url_for(locale: 'cy'), lang: 'cy', hreflang: 'cy' %>
      <% end %>
    </li>
  </ul>
</div>
